<Page
    x:Class="ClearAIText.App.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required">

    <ScrollViewer Padding="20,12,20,16">
        <StackPanel Spacing="12" MaxWidth="700" HorizontalAlignment="Stretch">
            <!-- App Header -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,14">
                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="16" VerticalAlignment="Center">
                    <Image Grid.Column="0" Source="ms-appx:///Assets/Square150x150Logo.png" Width="48" Height="48" VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1" Spacing="2">
                        <TextBlock x:Name="AboutTitleText" Text="Clear AI Text" Style="{StaticResource TitleTextBlockStyle}" FontWeight="Bold" TextWrapping="NoWrap" />
                        <TextBlock x:Name="AboutVersionText" Text="Версия 1.0.0 (x64 / ARM64)" Style="{StaticResource BodySubtleTextBlockStyle}" />
                        <TextBlock x:Name="AboutSubtitleText" Text="Высокопроизводительный фоновый сервис нормализации и безопасной очистки типографики буфера обмена Windows."
                                   Style="{StaticResource BodyTextBlockStyle}" Margin="0,2,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Features and Details -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,14">
                <StackPanel Spacing="10">
                    <TextBlock x:Name="TechStackHeader" Text="Технический стек и архитектура" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />

                    <StackPanel Spacing="4">
                        <TextBlock x:Name="Feature1Title" Text="100% Приватность и Безопасность" FontWeight="SemiBold" />
                        <TextBlock x:Name="Feature1Desc" Text="Полностью локальная обработка. Нулевая телеметрия, отсутствие сетевых запросов и автоматический пропуск защищенных менеджеров паролей." Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock x:Name="Feature2Title" Text="Нулевая задержка (Sub-millisecond)" FontWeight="SemiBold" />
                        <TextBlock x:Name="Feature2Desc" Text="Оптимизированный C# 13 / .NET 10 пайплайн с нулевыми аллокациями и нативной Win32 интеграцией." Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Spacing="4">
                        <TextBlock x:Name="Feature3Title" Text="Трехуровневая модель Unicode" FontWeight="SemiBold" />
                        <TextBlock x:Name="Feature3Desc" Text="Строгое разделение безопасной типографики, деструктивной очистки и интерактивного обнаружения омоглифов." Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
